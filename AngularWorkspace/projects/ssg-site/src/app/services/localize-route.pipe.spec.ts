import { LocalizeRoutePipe } from './localize-route.pipe';

describe('LocalizeRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizeRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
